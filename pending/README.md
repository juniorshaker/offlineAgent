# Pending — 未解决问题暂存区（Offlineagent）

当 Agent 在对话中遇到以下情况时，应将问题记录到此目录：

1. 用户说"先不做""等一下""改天再说"
2. 讨论的方案已有结论但未开始编码
3. 工具调用失败且暂时无法修复（如缺少外部依赖）
4. 用户明确标记为"待跟进"的事项

文件命名：`YYYY-MM-DD_{简短描述}.md`

下次对话开始时，Agent 应扫描此目录并告知用户有哪些待处理事项。
