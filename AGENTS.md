# AGENTS.md — OfflineAgent 离线智能助手规则

- 默认使用中文与用户沟通；`agent_memory/` 下文档默认使用中文。
- 代码、变量名、接口名、目录名及技术标识遵循仓库原有风格，不因中文沟通而改动。
- 非简单任务先读相关文件，再决定改动方案，不要直接开改。
- 若任务涉及多文件、运行时逻辑、公开接口、复杂排查或预计跨多轮执行，先给出简短计划，再开始修改。
- 对多步骤或长期任务，优先将需求转成可验证的成功标准、范围边界、停止条件和验证方式。
- 默认最小改动，不做无关重构，不扩大改动范围。
- 优先复用现有实现、组件、工具和模式。
- 每次改动都应能追溯到用户请求、已确认假设或必要验证；发现无关问题只记录风险，不顺手重构或扩大范围。
- 使用 `agent_memory/context.md` 维护项目级上下文，使用 `agent_memory/progress.md` 维护当前任务进度，使用 `agent_memory/bugs.md` 维护问题与风险记录。
- 非简单任务开始前先读取这三个文件；出现阶段完成、方向变化、失败尝试、准备结束会话时必须更新相关文件。
- 这三个文件只保留当前有效信息，不写成无限追加日志。
- 文件过长时先压缩为摘要，再归档旧内容到 `agent_memory/archive/`。
- 若改动涉及运行时代码、构建行为、测试、类型、公开接口或关键 UI 行为，必须执行与改动范围匹配的验证。
- 优先运行最小必要验证，并明确说明已验证和未验证部分。
- 若需求有歧义，优先采用最保守、最小范围、最容易验证的方案，并说明假设。
- 不要默默选择有风险的解释；若存在多个合理理解，先说明假设、权衡和建议方案，必要时询问用户。
- 不要把猜测当结论；未确认的内容必须明确标注。
- 同类问题若重复出现，再把经验固化进本文件。
- 若某类流程反复出现，不要继续堆到 `AGENTS.md`，优先升级为独立 skill。
- 保持本文件简短、稳定、可执行。

---

# AGENTS.md — OfflineAgent 项目规则

## 项目概述

OfflineAgent 是一个可在内网离线环境运行的 AI Agent，基于 Harness Engineering 分层架构，集成 Hermes Agent 的 Skill 管理体系。支持 CLI 和 Web 两种交互模式，兼容 Codex/OpenClaw 的 SKILL.md 格式。

## 项目结构

```
Offlineagent/
├── python/                     # 便携版 Python（可选）
├── browsers/                   # Playwright 浏览器二进制（可选）
├── setup.bat                   # 一键环境检查 + 启动
├── agent.py                    # CLI 入口
├── server.py                   # Web 入口
├── config.yaml                 # 统一配置
├── frontend/                   # Web UI
│   ├── index.html
│   ├── style.css
│   ├── app.js
│   └── vendor/
├── vendor/                     # 内置依赖
├── prompt_layer/               # 第1层：Prompt
├── orchestrator/               # 第2层：Orchestrator
├── tool_layer/                 # 第3层：Tool
├── evaluation/                 # 第4层：Evaluation
├── metrics/                    # 第5层：Metrics
├── memory_layer/               # 成长层
├── memory/                     # 运行时数据
├── skills/                     # Skills 目录
├── templates/                  # 文档模板
├── output/                     # 生成文档输出
├── log/                        # 运行日志
├── tests/                      # 测试用例
└── MANUAL.md                   # 使用手册
```

## 核心机制

### Skills 体系
- 多来源加载：`./skills`、`~/.codex/skills`、`~/.openclaw/skills`
- 两级加载：L1 索引常驻上下文，L2 正文按需注入
- 平台过滤：非当前 OS 的 skill 自动跳过
- 目录排除：自动跳过 `.git`、`node_modules` 等

### 错误自愈
- LLM 报错 → 记录模型名 + 错误类型 → 写入 `model_capabilities.json`
- 下次同模型自动预剥离不兼容内容
- 剥离后自动重试，用户无感知

### Token 节省
- Skills 两级加载
- Stable prompt 层磁盘缓存
- 对话摘要压缩（超 70% 触发）
- 循环检测强制终止

### 话题偏移检测
- 每轮调 LLM 判断话题一致性
- 偏移时询问用户是否开启新对话

## 内置命令

| 命令 | 功能 |
|------|------|
| `/help` | 帮助列表 |
| `/skills` | 已加载 skills 索引 |
| `/skill <name>` | 加载 skill 完整正文 |
| `/tools` | 列出可用工具 |
| `/status` | Token 预算仪表板 |
| `/memory` | 历史记忆列表 |
| `/clear` | 清空对话历史 |
| `/config` | 当前配置摘要 |
| `/exit` | 退出（触发摘要+反馈） |

## 代码开发完成标准

所有代码开发任务必须经过以下两个阶段才算完成，缺一不可：

### 阶段一：代码审查
- 审查代码逻辑是否正确，是否覆盖了边界条件
- 审查代码风格是否与项目现有风格一致
- 审查是否有冗余代码、死代码、未使用的变量/导入
- 审查错误处理是否完善（网络异常、文件不存在、权限不足等）
- 审查是否有安全隐患（路径遍历、命令注入、敏感信息泄露等）
- 审查通过后才能进入阶段二

### 阶段二：代码自测
- 必须编写并运行单元测试，覆盖核心逻辑和边界条件
- 所有测试用例必须全部通过（0 failure）
- 如涉及 CLI，必须验证命令行参数和交互流程
- 如涉及 API/Server，必须验证请求/响应流程
- 自测通过后，任务才算完成

### 完成标志
- 审查通过 + 测试全绿 = 完成
- 任一阶段不通过 = 继续修改，不可跳过
- 最终向用户汇报审查结论和测试结果

## 知识获取与技能扩展规则

当用户提出的问题我无法直接回答或不具备相关能力时，按以下优先级逐级处理：

### 优先级链
1. **search-first skill** — 先调用已有的 search-first skill，在本地 skill 库和社区仓库中搜索相关能力
2. **Skill 社区 / 插件库** — 如果 search-first 未找到，查询 Codex skill 社区和在线插件库
3. **安装询问** — 如果找到可用的 skill 或插件但尚未安装，必须向用户说明其功能、来源、安装影响，经用户明确同意后方可安装
4. **互联网搜索** — 如果社区和插件库都没有现成方案，通过网络搜索获取最佳实践和解决方案
5. **自写 Skill** — 如果以上步骤均无合适方案，基于搜索结果向用户提出编写自定义 skill 的建议，经用户同意后再动手编写

### 原则
- 绝不跳过询问环节直接安装任何 skill 或插件
- 绝不跳过搜索环节直接自造方案
- 每一步的输出都要让用户知情
- 优先复用已有方案，减少重复造轮子

## 路径访问规则

当用户给出具体文件路径或目录路径时，直接访问目标路径，不要逐层探索。只有直接访问失败时才回退到逐层遍历的方式。

## 配置文件注意事项

- `config.yaml` 中的 `llm.url` 是占位符，部署时必须改为实际内网 API 地址
- 两种对接模式：`primary`（JSON API 模式）和 `openai`（标准 OpenAI 接口，需 API Key）
- 修改配置后需重启 Agent
